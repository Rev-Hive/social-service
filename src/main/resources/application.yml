server:
  port: 8084

spring:
  application:
    name: social-service

  datasource:
    # Database Configuration (H2 in-memory database by default for local testing)
    url: jdbc:h2:mem:socialdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Uncomment and configure these for MySQL database connection
#   datasource:
#     url: jdbc:mysql://localhost:3306/revhive_social_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
#     username: sourav
#     password: sourav@123
#   jpa:
#     database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka
