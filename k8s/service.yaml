apiVersion: v1
kind: Service

metadata:
  name: social-service
  namespace: revhive

spec:
  selector:
    app: social-service

  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084

  type: ClusterIP
